int sensorPin = A3;
int LED = 9;
int sensorValue = 0;

void setup()
{
  pinMode(LED, OUTPUT);
}

void loop
{
  int pot = analogRead(sensorPin);

  pot = map(pot, 0, 1023, 0, 255);

  analogWrite(LED, pot);

  delay(250);
}
